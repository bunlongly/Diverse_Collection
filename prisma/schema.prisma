generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

model TestProfile {
  id  String @id @default(uuid()) @map("_id")
  name String

  @@map("test_profiles")
}
